apiVersion: v1
kind: Service
metadata:
  name: genai-app
  namespace: genai
spec:
  selector:
    app: genai-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer